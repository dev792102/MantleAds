{
  "name": "ad-registry-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MantleAds AdRegistry",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network mantleSepolia",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network mantle",
    "test": "npx hardhat test",
    "verify:sepolia": "npx hardhat verify --network mantleSepolia",
    "verify:mainnet": "npx hardhat verify --network mantle"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "ethers": "^6.8.0"
  }
}